{
  "routes": [
    { "handle": "filesystem" },
    { "src": "/auth/callback", "dest": "/login.html" }, // Redirige a tu página de login
    { "src": ".*", "dest": "/login.html" } // Sirve login.html para todas las rutas
  ]
}
